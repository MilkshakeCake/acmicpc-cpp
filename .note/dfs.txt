vector<int> graph[100001];
bool visited[100001] = {0, };
int result[100001];
int cnt = 0;

void dfs(int r) {
    if(visited[r]) return;

    visited[r] = true;
    result[r] = ++cnt;

    for(int i = 0; i < sz(graph[r]); i++) {
        dfs(graph[r][i]);
    }
}